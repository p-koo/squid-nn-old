from setuptools import setup, find_packages


setup(
    name="squid",
    version="0.1.0",
    packages=find_packages(),
    description = "Code to perform surrogate modeling analysis of deep learning models trained on genomics datasets.",
    python_requires=">=3.7",
    install_requires=[],
)
